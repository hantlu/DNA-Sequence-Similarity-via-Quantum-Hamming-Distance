This project compares classical Hamming distance with a quantum-inspired Hamming distance estimation using a Swap Test circuit implemented in Qiskit.

Random DNA sequences are generated and encoded into quantum states. The similarity between sequences is then computed using both:
a classical approach (direct comparison),
a quantum approach (state overlap estimation via a swap test).

The results are visualized and statistically analyzed.
